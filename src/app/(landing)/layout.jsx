import Header from "./_components/hero-banner";

export default function Layout({ children }) {
    return (
        <>
            {/* header */}
            {children}
            {/* footer */}
        </>
    );
}
